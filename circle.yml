machine:
    environment:
        CIRCLE_ENV: test
        DATABASE_URL: postgresql://admin:admin@localhost/postgres

test:
    override:
        - python -m unit_tests
deployment:
    production:
        branch: master
        heroku:
            appname: hidden-fjord-13641